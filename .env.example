# Copy to `.env.local` and adjust (`.env.local` is gitignored).
#
# To disable Next.js telemetry:
# Set NEXT_TELEMETRY_DISABLED=1 in `.env.local` to disable.
#
# Mock UI data (default) — safe for design / Playwright:
# NEXT_PUBLIC_USE_MOCK=true
#
# Real Conceal wallet (IndexedDB + public daemons):
NEXT_PUBLIC_USE_MOCK=false

# Real-mode wallet engine (only read when NEXT_PUBLIC_USE_MOCK=false):
#   wallet-core (default) — the legacy in-browser engine (lib/wallet-core + vendored globals).
#   sdk                   — the conceal-wallet-sdk engine (lib/services/real-sdk).
# NEXT_PUBLIC_WALLET_ENGINE=sdk

# GitHub Pages subpath when exporting (CI sets this):
# PAGES_BASE_PATH=/conceal-next-wallet
# NEXT_PUBLIC_PAGES_BASE_PATH=/conceal-next-wallet

# Donation page — fiat amount block + payment method toggles (server env, read at build/render):
# DONATION_SUPPORT_FIAT=OFF hides the "Choose your donation" section (default: OFF).
# DONATION_SUPPORT_METHOD=Crypto comma-separated keys: crypto, paypal, apple, visa (default: crypto).
