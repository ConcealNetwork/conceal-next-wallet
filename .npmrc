# Block package versions published in the last 7 days (supply-chain hardening).
# Applies to npm install / npm update — not npm ci (lockfile is installed verbatim).
min-release-age=7

audit=true
fund=false
