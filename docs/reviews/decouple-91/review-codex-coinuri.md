No real issues found.

- Class to object literal: clean. All moved methods reference `CoinUri.*`, not `this`, so extraction from `class CoinUri { static ... }` does not change method self-reference behavior (`lib/ui/coin-uri.ts:49`, `lib/ui/coin-uri.ts:69`, `lib/ui/coin-uri.ts:110`, `lib/ui/coin-uri.ts:142`, `lib/ui/coin-uri.ts:152`, `lib/ui/coin-uri.ts:202`, `lib/ui/coin-uri.ts:218`). Object initialization is also safe because those references run only when methods are called, after `CoinUri` is assigned.
- Return shapes: clean. `DecodedTxUri` preserves the old inline shape exactly: required `address`, optional `paymentId`, `recipientName`, `amount`, and `description` (`lib/ui/coin-uri.ts:23`). `DecodedWalletUri` preserves required `address` plus optional `spendKey`, `viewKey`, `mnemonicSeed`, `height`, `nonce`, and `encryptMethod` (`lib/ui/coin-uri.ts:32`). The decode assignments still populate the same fields from the same query keys (`lib/ui/coin-uri.ts:73`, `lib/ui/coin-uri.ts:161`).
- Encoder strings: clean. The template literal replacements preserve the old string concatenation output for normal inputs and the null gates are unchanged (`lib/ui/coin-uri.ts:133`, `lib/ui/coin-uri.ts:221`). `height: number | null` still omits only `null`; an explicit runtime `undefined` would still pass the `!== null` guard and stringify as `undefined`, matching the old `+ height` behavior (`lib/ui/coin-uri.ts:223`).
- Errors, prefixes, and edge cases: clean. Bare string throws remain the same (`missing_prefix`, `missing_address`, `invalid_address_length`, `missing_seeds`), `COIN_URI_PREFIX` is still used for `conceal:` tx decoding, and wallet import still accepts both `conceal.` and `conceal:` (`lib/ui/coin-uri.ts:57`, `lib/ui/coin-uri.ts:60`, `lib/ui/coin-uri.ts:67`, `lib/ui/coin-uri.ts:71`, `lib/ui/coin-uri.ts:145`, `lib/ui/coin-uri.ts:153`, `lib/ui/coin-uri.ts:159`, `lib/ui/coin-uri.ts:197`).
- Shim and callers: clean. `lib/wallet-core/CoinUri.ts:5` re-exports the moved symbol, so engine-internal `./CoinUri` imports in `lib/wallet-core/WalletRepository.ts:23` and `lib/wallet-core/wallet-operations.ts:11` still resolve. UI importers now use the neutral module directly (`app/(wallet)/wallet/receive/page.tsx:23`, `lib/ui/payment-link.ts:4`, `lib/ui/parse-scanned-send-payload.ts:1`, `lib/ui/wallet-export-pdf.ts:148`, `tests/coin-uri.test.ts:3`).

Verification:
- `npm test -- tests/coin-uri.test.ts tests/payment-link.test.ts tests/parse-scanned-send-payload.test.ts` (38 passed)
- `npm run types`
