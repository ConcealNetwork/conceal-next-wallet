[LOW] app/(wallet)/wallet/deposits/deposits-page-client.tsx:1063 - The per-deposit USD sublines render as `≈ $1.2345` while the new summary sublines render as `≈ $1.2345 USD` at line 422 and Send uses the same explicit `USD` suffix for its inline fiat helper text. That makes the same Deposits feature use two different fiat label formats, and the per-card rows are the odd ones out against the new summary UI and Send page convention. Concrete fix: append ` USD` in `DepositDetail`'s USD `<dd>` so Principal, Est. Interest, and Value at Maturity match the summary cards and Send fiat sublines.

[LOW] app/(wallet)/wallet/deposits/deposits-page-client.tsx:317 - The new USD sublines have no regression coverage for the two behaviors this feature depends on: hiding fiat when `price <= 0` and rendering the correct `CCX amount * price` value when market data is positive. The changed paths cover both summary totals here and per-card values at lines 856, 863, and 869, but the current test suite has no Deposits page UI coverage for these displays, so a future refactor could drop the `price > 0` gate or multiply the wrong field while existing tests still pass. Concrete fix: add a focused Deposits page/component test with mocked deposits and mocked `useMarketData` results for `price: 0` and a positive price, asserting the USD rows are absent in the first case and that Total Locked, Total Est. Interest, Principal, Est. Interest, and Value at Maturity render the expected `formatUsd(ccx * price)` labels in the second.
