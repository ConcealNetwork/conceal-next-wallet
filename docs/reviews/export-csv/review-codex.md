[LOW] tests/transaction-csv.test.ts:113 — The formula-injection unit coverage only exercises the payload battery through `Message`, with one extra assertion for `Payment ID`; it never proves that the other exported untrusted string cells (`Address`, and defensively `Hash`) are protected. The current serializer happens to guard them through the generic non-amount path, but a later column-specific refactor could stop guarding `Address` while this suite still passes, despite the contract calling address attacker-influenced. Concrete fix: make the injection test table-driven over every non-trusted exported string column, including `Address`, `Payment ID`, `Hash`, and `Message`, and assert the decoded cell is apostrophe-prefixed for each trigger payload while the amount columns remain unprefixed.

[LOW] e2e/export-csv.spec.ts:41 — The filtered-export E2E only asserts that the downloaded CSV has fewer rows than the full mock list, so exporting an arbitrary subset or the current visible page could still pass. It also omits the contract's browser-level security gate that no decoded exported cell starts with a formula trigger. Concrete fix: parse the downloaded CSV in the E2E test, assert the Sent tab export rows are actually the filtered sent rows (for example by `Type`/`Direction` and known mock count), and add a fixture or route override with formula-shaped address/payment/message values so the downloaded file is checked for decoded cell safety in the browser path.
