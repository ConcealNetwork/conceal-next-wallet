- [MEDIUM] app/(wallet)/wallet/deposits/deposits-page-client.tsx:209 - Deposit creation still has no view-only short-circuit at the page-level mutation boundary. The visible "Create New Deposit" buttons are disabled when `viewOnly` is true, but `CreateDepositDialog` remains mounted, `confirmCreate()` calls `onCreate()` unconditionally at line 1287, and the page callback immediately calls `createDeposit.mutate()` here. A stale already-open dialog, restored component state, or direct handler invocation can still attempt a create path and rely on the service error after the user reaches confirmation, which violates the spec's "handler short-circuit" requirement for deposits. Concrete fix: pass `viewOnly` into `CreateDepositDialog`, include it in the Review/Confirm disabled states, and guard both `submitForm()`/`confirmCreate()` or the `onCreate` callback with `toast.error(walletCopy.viewOnlyDepositDisabled); return;`.

- [MEDIUM] lib/services/mock/wallet.service.ts:39 - Normal mock `openWallet()` preserves the last imported view-only singleton state instead of opening a spend-capable mock wallet. A view-only keys import sets `mockViewOnly` at lines 59-63, and a later "Open Wallet" returns `currentWalletInfo()` without resetting it here. That makes mock mode diverge from the expected normal-wallet path and also makes `e2e/view-only-mode.spec.ts:50` depend on test ordering/isolation even though the prior test imports view-only. Concrete fix: reset `mockViewOnly = false` in `openWallet()` for the normal mock wallet path, or explicitly model separate mock wallets and reset state in Playwright `beforeEach`.

- [LOW] app/(wallet)/wallet/settings/page.tsx:254 - The settings optimize trigger is still view-only unaware. The service guard blocks `optimizeWallet()`, but the UI continues to show "Optimization can be attempted" at lines 316-319 and leaves the button enabled whenever `optimizationNeeded` and sync state allow it at lines 299-308. The spec explicitly says to guard the optimize trigger and suppress the optimize prompt for view-only wallets. Concrete fix: read `useWalletViewOnly()`, include `viewOnly` in the button disabled expression, short-circuit `handleOptimize()` with `walletCopy.viewOnlyOptimizeDisabled`, and hide/replace the optimization-needed prompt when view-only.

- [LOW] app/(wallet)/wallet/export/page.tsx:91 - The export screen still labels and reveals the spend key field exactly like a full wallet even when the exported spend key is blank. `exportWalletOperation()` returns `spendKey: wallet.keys.priv.spend` at `lib/wallet-core/wallet-operations.ts:324`, so a view-only wallet shows an empty spend key with no explanation, missing the spec's copy-only note that "Spend key is blank — view-only wallet." Concrete fix: when `data?.spendKey === ""`, render the view-only note next to the Spend key field and include equivalent wording in copied/PDF export formatting if those surfaces display the blank spend key.
